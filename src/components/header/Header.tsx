import Navbar from "../navbar/Navbar";
import { StyledHeader } from "../styles/Header.styled";

const Header = () => {
  return (
    <StyledHeader>
      <Navbar />
    </StyledHeader>
  );
};

export default Header;
